<p id="notice"><%= notice %></p>
<div class="page-header">
  <h1>Books</h1>
</div>
<table class ="table">
  <thead>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Category</th>
    <th>Created</th>
    <th>Updated</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.description %></td>
        <td><%= book.category_id %></td>
        <td><%= book.created_at %></td>
        <td><%= book.updated_at %></td>
        <td><%= link_to 'Show', book %></td>
        <% if current_user %>
            <td><%= link_to 'Edit', edit_rating_path(book) %></td>
            <td><%= link_to 'Destroy', book, method: :delete, data: {confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
  <% end %>
  </tbody>
</table>

<br>
