<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <p><strong><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</strong></p>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, id: :rating_score %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id, id: :rating_book_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :rating_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
