<%= form_with(model: rating, remote: true) do |form| %>
  <% if rating.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <p><strong><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</strong></p>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= select_tag('rating[score]', options_for_select([1,2,3,4,5,6]),
                   {:class => 'form-control'}) %>
    <% if re_rate(:book_id) %>
      <%= form.hidden_field :book_id, id: :rating_book_id %>
      <%= form.hidden_field :user_id, id: :rating_user_id %>
    <% else %>
      <%= form.hidden_field :book_id, id: :rating_book_id %>
      <%= form.hidden_field :user_id, id: :rating_user_id %>
    <% end %>

    <td> <%= form.submit :class => 'btn btn-success actions'%></td>
<% end %>
